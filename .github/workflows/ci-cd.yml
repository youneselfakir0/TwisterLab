name: TwisterLab CI/CDname: CI/CD Workflow



on:on:

  push:  push:

    branches: [main, develop]    branches:

  pull_request:      - main

    branches: [main]  pull_request:

    branches:

env:      - main

  PYTHON_VERSION: "3.11"

  REGISTRY: ghcr.iojobs:

  IMAGE_NAME: ${{ github.repository }}  build:

    runs-on: ubuntu-latest

jobs:

  # ============================================    steps:

  # JOB 1: Lint & Security      - name: Checkout code

  # ============================================        uses: actions/checkout@v4

  lint:

    name: 🔍 Lint & Format      - name: Set up Python

    runs-on: ubuntu-latest        uses: actions/setup-python@v4

    steps:        with:

      - uses: actions/checkout@v4          python-version: '3.11'



      - name: Set up Python      - name: Install dependencies

        uses: actions/setup-python@v5        run: |

        with:          python -m pip install -r requirements.txt

          python-version: ${{ env.PYTHON_VERSION }}          pip install -e .

          cache: 'pip'      - name: Cache pip

        uses: actions/cache@v4

      - name: Install linters        with:

        run: pip install ruff mypy          path: ~/.cache/pip

          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      - name: Ruff Lint          restore-keys: |

        run: ruff check src/twisterlab/ --output-format=github            ${{ runner.os }}-pip-

        continue-on-error: true

      - name: Run tests

      - name: Ruff Format Check        run: |

        run: ruff format --check src/twisterlab/          pytest tests/ -v --maxfail=1 -m "not e2e"

        continue-on-error: true

      - name: Run tests with coverage

  security:        run: |

    name: 🔐 Security Scan          pytest --maxfail=1 -m "not e2e" --cov=src/twisterlab --cov-report=xml --cov-report=term --cov-fail-under=60

    runs-on: ubuntu-latest

    steps:      - name: Upload coverage report

      - uses: actions/checkout@v4        if: always()

        uses: actions/upload-artifact@v4

      - name: Set up Python        with:

        uses: actions/setup-python@v5          name: coverage-report

        with:          path: coverage.xml

          python-version: ${{ env.PYTHON_VERSION }}

          cache: 'pip'      - name: Upload to Codecov

        if: secrets.CODECOV_TOKEN != ''

      - name: Install security tools        uses: codecov/codecov-action@v4

        run: pip install bandit safety        with:

          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Bandit Security Scan          files: coverage.xml

        run: bandit -r src/twisterlab/ -ll -ii          flags: unittests

        continue-on-error: true          fail_ci_if_error: true



      - name: Safety Dependency Check      - name: Run TwisterLang lint

        run: safety check -r requirements.txt        run: |

        continue-on-error: true          python twisterlang_lint.py --all



  # ============================================      - name: Run static checks

  # JOB 2: Tests        run: |

  # ============================================          ruff check src/twisterlab tests/

  test:          mypy src/twisterlab

    name: 🧪 Tests      - name: Run pre-commit hooks (fast check)

    runs-on: ubuntu-latest        run: |

    needs: [lint, security]          python -m pip install pre-commit

              python -m pre_commit run check-pytestmark-duplicates --all-files || true

    services:          # Don't fail the build on pre-commit run here; we still enforce checks below

      postgres:      - name: Run pre-commit hooks (enforce)

        image: postgres:16        run: |

        env:          python -m pip install pre-commit

          POSTGRES_USER: twisterlab          python -m pre_commit run --all-files

          POSTGRES_PASSWORD: test_password      - name: Check duplicate pytestmark assignments

          POSTGRES_DB: twisterlab_test        run: |

        ports:          python scripts/check_pytestmark_duplicates.py

          - 5432:5432

        options: >-      - name: Build Docker images

          --health-cmd pg_isready        run: |

          --health-interval 10s          docker build -t twisterlab-api:latest -f Dockerfile.api .

          --health-timeout 5s          docker build -t twisterlab-mcp:latest -f Dockerfile.mcp .

          --health-retries 5

      - name: Run Alembic migrations (validate)

      redis:        run: |

        image: redis:7-alpine          python -m pip install alembic

        ports:          # Use a local sqlite file for validating migrations

          - 6379:6379          export PYTHONPATH=src

        options: >-          export DATABASE_URL=sqlite:///./alembic_test.db || $env:DATABASE_URL='sqlite:///./alembic_test.db'

          --health-cmd "redis-cli ping"          alembic -c alembic.ini upgrade head

          --health-interval 10s          alembic -c alembic.ini downgrade base

          --health-timeout 5s

          --health-retries 5      - name: Run API container for integration tests

        run: |

    steps:          # Run the built API image in the background

      - uses: actions/checkout@v4          docker run -d --name twisterlab-api-test -p 8000:8000 twisterlab-api:latest || docker ps -a

          # Wait for the server to be ready (max ~60s)

      - name: Set up Python          for i in $(seq 1 30); do

        uses: actions/setup-python@v5            if curl -fsS http://localhost:8000/ >/dev/null 2>&1; then

        with:              echo "API started"

          python-version: ${{ env.PYTHON_VERSION }}              break

          cache: 'pip'            fi

            echo "Waiting for API... ($i)"

      - name: Install dependencies            sleep 2

        run: |          done

          pip install --upgrade pip          # Test the static UI and metrics endpoints

          pip install -e ".[dev]" || pip install -r requirements.txt          echo "Testing /ui..."

          pip install pytest pytest-cov pytest-asyncio httpx          curl -fsS http://localhost:8000/ui/ -o /tmp/ui.out

          if ! grep -q "TwisterLab Browser UI" /tmp/ui.out; then

      - name: Run Tests            cat /tmp/ui.out

        env:            exit 1

          DATABASE_URL: postgresql://twisterlab:test_password@localhost:5432/twisterlab_test          fi

          REDIS_URL: redis://localhost:6379/0          echo "Testing /metrics..."

          TESTING: "true"          curl -fsS http://localhost:8000/metrics -o /tmp/metrics.out

        run: |          if [ ! -s /tmp/metrics.out ]; then

          pytest tests/ -v --cov=src/twisterlab --cov-report=xml -m "not e2e" --maxfail=3            echo "Metrics endpoint returned no content"

        continue-on-error: true            cat /tmp/metrics.out

            exit 1

      - name: Upload Coverage          fi

        uses: codecov/codecov-action@v4          # Leave the container running for further smoke tests

        with:          echo "API container started and smoke-checked."

          files: ./coverage.xml

          fail_ci_if_error: false      - name: Run Alembic autogenerate check

        continue-on-error: true        run: |

          python -m pip install alembic

  # ============================================          export DATABASE_URL=sqlite:///./alembic_test.db || $env:DATABASE_URL='sqlite:///./alembic_test.db'

  # JOB 3: Build Docker Images          # ensure models can be autogen by alembic

  # ============================================          alembic -c alembic.ini revision --autogenerate -m 'CI autogenerate check' --rev-id temp || true

  build:          # cleanup temporary revision if it exists

    name: 🐳 Build Docker          if [ -f alembic/versions/temp.py ]; then rm alembic/versions/temp.py; fi

    runs-on: ubuntu-latest

    needs: test      - name: Run runtime smoke tests (pytest)

    if: github.event_name != 'pull_request'        run: |

    permissions:          # Ensure we have the test dependencies

      contents: read          pip install -r requirements.txt

      packages: write          # In case an earlier container was dangling, remove it

          docker rm -f twisterlab-api-test-smoke || true

    strategy:          # Start a fresh container for the pytest runtime tests

      matrix:          docker run -d --name twisterlab-api-test-smoke -p 8000:8000 twisterlab-api:latest

        include:          # Wait for container to become available

          - dockerfile: Dockerfile.api          for i in $(seq 1 30); do

            image: api            if curl -fsS http://localhost:8000/ >/dev/null 2>&1; then

          - dockerfile: Dockerfile.mcp              echo "API started for pytest"

            image: mcp              break

            fi

    steps:            echo "Waiting for API (pytest)... ($i)"

      - uses: actions/checkout@v4            sleep 2

          done

      - name: Set up Docker Buildx          pytest -q tests/integration/test_smoke_runtime_endpoints.py -q

        uses: docker/setup-buildx-action@v3          # Stop and delete the test container

          docker stop twisterlab-api-test-smoke || true

      - name: Login to GitHub Container Registry          docker rm twisterlab-api-test-smoke || true

        uses: docker/login-action@v3

        with:      - name: Push Docker images

          registry: ${{ env.REGISTRY }}        env:

          username: ${{ github.actor }}          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

          password: ${{ secrets.GITHUB_TOKEN }}          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

        run: |

      - name: Docker Metadata          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

        id: meta          docker push twisterlab-api:latest

        uses: docker/metadata-action@v5          docker push twisterlab-mcp:latest

        with:

          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.image }}      - name: Deploy to Kubernetes

          tags: |        env:

            type=ref,event=branch          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}

            type=sha,prefix=        run: |

            type=raw,value=latest,enable={{is_default_branch}}          echo "${KUBE_CONFIG}" | base64 --decode > kubeconfig

          kubectl --kubeconfig=kubeconfig apply -f k8s/deployments/twisterlab-api.yaml

      - name: Build and Push          kubectl --kubeconfig=kubeconfig apply -f k8s/deployments/twisterlab-mcp.yaml

        uses: docker/build-push-action@v5

        with:  playwright-e2e:

          context: .    runs-on: ubuntu-latest

          file: ${{ matrix.dockerfile }}    needs: build

          push: true    timeout-minutes: 20

          tags: ${{ steps.meta.outputs.tags }}    steps:

          labels: ${{ steps.meta.outputs.labels }}      - name: Checkout code

          cache-from: type=gha        uses: actions/checkout@v4

          cache-to: type=gha,mode=max

      - name: Set up Python

  # ============================================        uses: actions/setup-python@v4

  # JOB 4: Deploy        with:

  # ============================================          python-version: '3.11'

  deploy:

    name: 🚀 Deploy      - name: Cache pip

    runs-on: ubuntu-latest        uses: actions/cache@v4

    needs: build        with:

    if: github.ref == 'refs/heads/main'          path: ~/.cache/pip

    environment:          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      name: production          restore-keys: |

      url: http://edgeserver.twisterlab.local:30001            ${{ runner.os }}-pip-

      - name: Install dependencies

    steps:        run: |

      - uses: actions/checkout@v4          python -m pip install -r requirements.txt

          pip install -e .

      - name: Deploy notification

        run: |      - name: Cache Playwright browsers

          echo "🚀 Deployment triggered for TwisterLab"        uses: actions/cache@v4

          echo "Branch: ${{ github.ref_name }}"        with:

          echo "Commit: ${{ github.sha }}"          path: ~/.cache/ms-playwright

          echo "Images pushed to: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"          key: ${{ runner.os }}-playwright-${{ hashFiles('**/pyproject.toml', '**/requirements.txt') }}

          restore-keys: |
            ${{ runner.os }}-playwright-
      - name: Setup Playwright (GH Action)
        uses: microsoft/playwright-github-action@v1
        with:
          browsers: chromium
      - name: Install Playwright browsers
        run: |
          python -m playwright install --with-deps chromium

      - name: Build API Docker image
        run: |
          docker build -t twisterlab-api:latest -f Dockerfile.api .

  build-windows:
    runs-on: windows-latest
    needs: build
    steps:
      - name: Checkout code (Windows)
        uses: actions/checkout@v4

      - name: Set up Python (Windows)
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip (Windows)
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies (Windows)
        run: |
          python -m pip install -r requirements.txt
          pip install -e .

      - name: Run tests (Windows)
        run: |
          pytest tests/ -v --maxfail=1 -m "not e2e"

      - name: Run static checks (Windows)
        run: |
          ruff check src/twisterlab tests/
          python -m mypy src/twisterlab

      - name: Run API container for Playwright tests
        run: |
          docker rm -f twisterlab-api-e2e || true
          docker run -d --name twisterlab-api-e2e -p 8000:8000 twisterlab-api:latest
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/ >/dev/null 2>&1; then
              echo "API started for playwright tests"
              break
            fi
            echo "Waiting for API (playwright)... ($i)"
            sleep 2
          done

      - name: Run Playwright e2e tests
        env:
          E2E: 1
          API_BASE_URL: http://localhost:8000
        run: |
          pytest -q -m e2e --junitxml=playwright_e2e_results.xml

      - name: Stop and remove API container
        run: |
          docker stop twisterlab-api-e2e || true
          docker rm twisterlab-api-e2e || true
      - name: Upload Playwright E2E junit xml
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-e2e-results
          path: playwright_e2e_results.xml
      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: artifacts/