# TwisterLab Documentation Consolidation Plan

This document summarizes the recommended consolidation for documentation and related steps to clean up historical, duplicated, or archived content.

## Objectives
- Reduce fragmentation and duplication across `*.md` and `*.txt` files
- Consolidate a handful of entry-point docs (GETTING_STARTED, ARCHITECTURE, DEPLOYMENT, API, OPERATIONS)
- Move historical session reports and archives into `docs/archive/sessions` to declutter the main repo

## Summary of findings (tools/docs_audit.py)
- Total scanned docs: 283
- Duplicated hashes found: 22 (most are archived copies under `archive/` or `.continue/` backups)
- Top directories with many docs: `.` (root), `archive/docs/ARCHITECTURE`, `.continue`, `docs/archive`, `docs/OPERATIONS`

## High-level plan (3 phases)
1. Phase 1 - Cleanup & Archive (safe)
   - Move all `SESSION_*`, `REPORT_*`, `RAPPORT_*` files into `docs/archive/sessions` (use `infrastructure/scripts/archive_sessions.ps1` - DryRun first)
   - Move `archive/` copies into `docs/archive/archives` (optional - maintain an `archive/twisterlab-repo` snapshot)
   - Remove duplicates in root and `.continue` that are now archived.

2. Phase 2 - Consolidate top-level docs
   - Merge `QUICK_START`, `START_HERE`, `GETTING_STARTED` drafts into `docs/GETTING_STARTED.md`.
   - Merge `ARCHITECTURE_*` into `docs/ARCHITECTURE.md`.
   - Consolidate `DEPLOYMENT_*` into `docs/DEPLOYMENT.md`.
   - Consolidate API docs into `docs/API.md` and ensure the `api/` endpoints and OpenAPI schemas are updated.
   - Consolidate operational runbook into `docs/OPERATIONS.md`.

3. Phase 3 - Governance & CI
   - Add a docs linter (markdownlint) to CI to detect duplicates and missing frontmatter
   - Add a CI job that runs `tools/docs_audit.py` and fails if duplicates or a large number of unactioned session reports exist
   - Add `docs/index.md` or update `README.md` with links to consolidated docs

## Initial files to merge (suggested)
### `docs/GETTING_STARTED.md` (sources to consider):
  - README.md (Quick Start section)
  - archive/docs/GETTING_STARTED/QUICK_START.md
  - archive/docs/GETTING_STARTED/START_HERE.md
  - archive/docs/GETTING_STARTED/QUICKSTART_AGENT_IMPLEMENTATION.md

### `docs/ARCHITECTURE.md` (sources to consider):
  - ARCHITECTURE_V2_VISION.md
  - ORCHESTRATION_COMPLETE.md
  - archive/docs/ARCHITECTURE/*.md

### `docs/DEPLOYMENT.md`:
  - PRODUCTION_DEPLOYMENT_GUIDE.md
  - DEPLOYMENT.md (if present / archive)

### `docs/API.md`:
  - API.md (if present)
  - docs relating to MCP & REST API endpoints

### `docs/OPERATIONS.md`:
  - docs/OPERATIONS/*
  - scripts and runbook instructions

## Actionable next steps (recommended)
1. Review the `tools/reports/docs_inventory.json` and `tools/reports/duplicate_docs.csv` generated by audit and confirm files to include in consolidation.
2. Run `infrastructure/scripts/archive_sessions.ps1 -DryRun` (already ran) to confirm candidate archive files.
3. Approve `archive_sessions.ps1` to perform actual move (run without `-DryRun`) or keep DryRun until manual review is complete.
4. Merge or draft `docs/GETTING_STARTED.md` by moving content from the listed sources into a single file (I can create a draft for review).

## Best practices for merging
- Preserve original authors & references at top of the merged files
- Keep functional sections short and add a "See also" to link deeper docs in `docs/advanced`
- Keep historical session files in `docs/archive` with a YYYYMMDD prefix for clarity

## Optional next tasks I can run
- Create a draft `docs/GETTING_STARTED.md` based on QuickStarts and README
- Create a PR with the archived session files moved (if you confirm) and PRs for the merged docs

Generated by tools/docs_audit.py on request
